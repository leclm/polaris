import { CapitalizacaoPipe } from './capitalizacao.pipe';

describe('CapitalizacaoPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizacaoPipe();
    expect(pipe).toBeTruthy();
  });
});
