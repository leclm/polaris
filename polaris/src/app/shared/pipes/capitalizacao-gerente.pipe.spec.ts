import { CapitalizacaoGerentePipe } from './capitalizacao-gerente.pipe';

describe('CapitalizacaoGerentePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizacaoGerentePipe();
    expect(pipe).toBeTruthy();
  });
});
