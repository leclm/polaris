import { CpfGerentePipe } from './cpf-gerente.pipe';

describe('CpfGerentePipe', () => {
  it('create an instance', () => {
    const pipe = new CpfGerentePipe();
    expect(pipe).toBeTruthy();
  });
});
