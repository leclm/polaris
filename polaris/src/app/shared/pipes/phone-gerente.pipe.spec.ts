import { PhoneGerentePipe } from './phone-gerente.pipe';

describe('PhoneGerentePipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneGerentePipe();
    expect(pipe).toBeTruthy();
  });
});
