import { DateGerentePipe } from './date-gerente.pipe';

describe('DateGerentePipe', () => {
  it('create an instance', () => {
    const pipe = new DateGerentePipe();
    expect(pipe).toBeTruthy();
  });
});
