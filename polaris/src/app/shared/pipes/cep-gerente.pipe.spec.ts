import { CepGerentePipe } from './cep-gerente.pipe';

describe('CepGerentePipe', () => {
  it('create an instance', () => {
    const pipe = new CepGerentePipe();
    expect(pipe).toBeTruthy();
  });
});
